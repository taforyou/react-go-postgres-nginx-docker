# ports :
#   - port ที่ข้างนอกใช้ Access เข้ามา : port ที่ภายในใช้เรียกหากัน (อ่านเพิ่มเติมของ Riffy มีบอก)
version: "1.0"
services:
  goservice:
    build: "."
    ports:
      - "1234:8080"
  nginx:
    build: "./nginx"
    ports:
      - "80:80"
    depends_on:
      - "goservice"
